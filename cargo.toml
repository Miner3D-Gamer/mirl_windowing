[package]
name = "mirl_windowing"
version = "0.0.0-alpha"
edition.workspace = true
repository.workspace = true
license.workspace = true
authors.workspace = true

# This is a neat tool we will use later
# [target.'cfg(all(not(target_arch = "wasm32")))'.dependencies]

[features]
default = ["std"]
# Core
std = []
_checking = ["all", "cursor_show_hotspot"]
all = [
    "glfw",
    "minifb",
    "console",
    "all_codecs",
    "all_enum_extensions",
    "image_support",
]

# Does nothing by itself. Needs another codec or enum flag to do something
mirl_derive = ["dep:mirl_derive"]
# Compatibility with ffi
c_compatible = ["mirl_derive"]
# Enables all codecs
all_codecs = ["serde", "bitcode", "wincode", "compactly", "zerocopy"]
# All enum extensions
all_enum_extensions = ["strum", "enum_ext"]

# Codec stuff
serde = ["dep:serde", "mirl_derive", "mirl_windowing_core/serde"]
bitcode = ["dep:bitcode", "mirl_derive", "mirl_windowing_core/bitcode"]
wincode = ["dep:wincode", "mirl_derive", "mirl_windowing_core/wincode"]
compactly = ["dep:compactly", "mirl_derive", "mirl_windowing_core/compactly"]
zerocopy = ["dep:zerocopy", "mirl_derive", "mirl_windowing_core/zerocopy"]

# Enum extensions
strum = ["dep:strum", "mirl_derive", "mirl_windowing_core/strum"]
enum_ext = ["dep:enum_ext", "mirl_derive", "mirl_windowing_core/enum_ext"]

# Frameworks
## Framework backend using minifb
minifb = ["mirl_windowing_minifb"]
## Framework backend using glfw
glfw = ["mirl_windowing_glfw"]
## Framework backend using the console
console = ["mirl_windowing_console"]

# keyboard_query = ["mirl_windowing_core/keyboard_query"]

cursor_show_hotspot = ["mirl_windowing_core/cursor_show_hotspot"]

image_support = ["dep:image"]

[dependencies]
mirl_windowing_core = { version = "0.0.0-alpha" }
mirl_buffer = { version = "0.0.0-alpha" }
mirl_derive = { version = "0.0.0-alpha", optional = true }
# Codec
compactly = { version = "0.1.6", optional = true }
serde = { version = ">=1.0", optional = true, default-features = false, features = [
    "derive",
] }
bitcode = { version = "0.6.9", optional = true, default-features = false, features = [
    "derive",
] }
wincode = { version = "0.5.3", optional = true, features = ["derive"] }
zerocopy = { version = "0.8.48", optional = true, default-features = false, features = [
    "float-nightly",
    "derive",
] }

# Enum functionality
strum = { version = ">=0.28", optional = true, default-features = false, features = [
    "derive",
] }
enum_ext = { version = "0.6.0", optional = true, default-features = false }


image = { version = ">=0.25.9", optional = true, default-features = false }

# Normal
mirl_extensions = { version = "0.0.0-alpha" }
[target.'cfg(all(not(target_arch = "wasm32")))'.dependencies]
mirl_windowing_glfw = { version = "0.0.0-alpha", optional = true }
mirl_windowing_minifb = { version = "0.0.0-alpha", optional = true }
mirl_windowing_console = { version = "0.0.0-alpha", optional = true }

[build-dependencies]
mirl_build_tools = { workspace = true }


[lints]
workspace = true
